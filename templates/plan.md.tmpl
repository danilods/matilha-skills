---
name: {{feature_slug}}
spec: {{spec_relative_path}}
created: "{{date}}"
waves:
  w1:
    - SP1
---

# {{feature_title}} — Execution Plan

> Scaffold generated by `matilha plan` on {{date}}. Fill wave/SP breakdown
> with disjoint file lists per SP, then run `/hunt` to dispatch worktrees.

## Wave 1

### SP1 — [placeholder title]
<!-- one sub-problem solvable by one generator in one commit. -->
[placeholder]

**Acceptance**
- [ ] [binary criterion, e.g., "migrations/001_foo.sql runs clean on empty DB"]

**Touches**
- `path/to/file.ts`

**Tests**
- `tests/path/file.test.ts`

## Done when

Each SP has SP-DONE.md written. Then run `/gather` to merge + regression + tag.

## Links
- Spec: `{{spec_relative_path}}`
- Methodology SoR: `methodology/40-execucao.md`
