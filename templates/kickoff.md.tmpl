# Kickoff — {{feature_slug}} / wave-{{wave_num_padded}} / {{sp_id}}

> **Worktree:** `{{worktree_path}}`
> **Branch:** `{{branch_name}}`
> **Main repo:** `{{main_repo_path}}`

## Your mission

**{{sp_title}}**

{{sp_description}}

### Acceptance (all must pass)
{{acceptance_checklist}}

### Files you own (do not touch anything outside this list)
{{touches_list}}

### Tests expected
{{tests_list}}

## Exit gates (complete before writing SP-DONE.md)

**Correctness**
- [ ] All acceptance items above are checked
- [ ] All expected tests exist and pass locally

**Discipline**
- [ ] No file touched outside "Files you own"
- [ ] Lint + typecheck green (if configured)

**Handoff**
- [ ] Commits follow convention (feat/fix/refactor/test/docs/chore)

## Context pointers
- Plan: `{{main_repo_path}}/docs/matilha/plans/{{feature_slug}}-plan.md`
- Spec: `{{main_repo_path}}/docs/matilha/specs/{{feature_slug}}-spec.md`
- Methodology SoR: `methodology/40-execucao.md` (fetch with `matilha pull methodology/40-execucao`)
- Project rules: `{{main_repo_path}}/CLAUDE.md`
- Wave status: `{{main_repo_path}}/docs/matilha/waves/wave-{{wave_num_padded}}-status.md`

{{#if superpowers_detected}}
## Recommended execution path (superpowers detected)

Invoke `Skill superpowers:executing-plans` with:
- Plan file: `{{main_repo_path}}/docs/matilha/plans/{{feature_slug}}-plan.md`
- Focused SP: `{{sp_id}}`

Superpowers is the preferred task-control engine when present. Matilha provides
the harness (worktree isolation, disjunction gates, wave-status tracking);
superpowers drives execution with TDD + review checkpoints.
{{else}}
## Execution path (no superpowers detected)

Matilha runs fully standalone:
1. Read your mission above.
2. Implement with TDD (failing test → minimal code → green → refactor).
3. Run full test suite between changes.
4. Commit atomically per logical change.
{{/if}}

## When done

1. Verify all exit gates pass.
2. Fill `SP-DONE.md` in this worktree root (template already rendered there).
3. Commit `SP-DONE.md`.
4. Return to main session or ping the orchestrator.

---
*Generated by `matilha hunt` — do not edit this file directly.*
